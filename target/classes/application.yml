hjjjjconfig:
  downloadBasePath: http://localhost:8888/api/Upload/

logging:

  file:
    name: log/log.log

  level:
    root: info
    com.example.netdisk: debug


server:
  port: 8888
  servlet:
    context-path: /api


spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 1024MB
      max-request-size: 1024MB

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: wsadjkl123
    url: jdbc:mysql://localhost:3306/netdisk?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT
    driver-class-name: com.mysql.cj.jdbc.Driver


mybatis:
  type-aliases-package: com.example.netdisk.pojo
  mapper-locations: classpath:/mybatis/*.xml















